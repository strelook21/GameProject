#include "Draw.h"
#include "Engine.h"
#include "Object.h"
#include <cmath>


void draw_sprited(unsigned int id) {
	//for (int i = std::round(std::fmax(0, objectList[id].Y() - objectList[id].offet)), i < std::round(std::fmax(SCREEN_HEIGHT))
}